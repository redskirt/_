# https://www.playframework.com/documentation/latest/Configuration

play.filters {
  headers {
    # The X-Frame-Options header. If null, the header is not set.
    #frameOptions = "DENY"

    # The X-XSS-Protection header. If null, the header is not set.
    #xssProtection = "1; mode=block"

    # The X-Content-Type-Options header. If null, the header is not set.
    #contentTypeOptions = "nosniff"

    # The X-Permitted-Cross-Domain-Policies header. If null, the header is not set.
    #permittedCrossDomainPolicies = "master-only"

    # The Content-Security-Policy header. If null, the header is not set.
   # contentSecurityPolicy = "default-src http:;"
   contentSecurityPolicy = null
  }
}

## JDBC Datasource
db {
  default.driver = "com.mysql.jdbc.Driver"
  default.url = "jdbc:mysql://miximixi.me/tunami"
  default.username = "root"
  default.password = "Redskirt0608_"

  # You can turn on SQL logging for any datasource
  # https://www.playframework.com/documentation/latest/Highlights25#Logging-SQL-statements
  #default.logSql=true
}

# Default database configuration
#slick.dbs.default.driver="scala.slick.driver.MySQLDriver"
#slick.dbs.default.db.driver="com.mysql.jdbc.Driver"
#slick.dbs.default.db.url= "jdbc:mysql://miximixi.me/tunami"
